//import

public:

enum Color{
    blue = 0x0,
    red = 1,
    green = 2,
    black = 3,
    gray = 4
}

struct Point{
    public:
    x: u32;
    y: u32;
}

const CONSTANT = 0xdeadbeef;

flowing fn add( num1: lend long, num2: long ){
    marker sample(x: u32){

    }

   if true
        entry goto sample(5);
   else
        goto scene level1;

   public struct Point{
       public:
       x: u32;
       y: u32;
   }
    union Number{
        u8,
        u16,
        f32,
        char
    }
    fn add(){
        return infinity;
    }
}


fn main( agrc: u32, argv: string ){

    let num1: u32 = 50;

    switch(a){
        case 1:
            break;
        default:
            println(5);
    }


}