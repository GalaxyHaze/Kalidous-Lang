//import

public:

enum Color{
    blue = 0x0,
    red = 1,
    green = 2,
    black = 3,
    gray = 4
}

public struct Point{
    x: u32,
    y: u32
}

const CONSTANT = 0xdeadbeef;

flowing fn add( num1: lend long, num2: long ){
    marker sample(x: u32){

    }

   if (true)
        entry goto sample(5);
   else
        goto scene level1;

   public struct Coord{
       x: u32,
       y: u32
   }
   //is a type constrain, inline union
    type Number = u8 | u16 | f32 | f64;
    fn add(a: Number, b: Number){
        a + b
    }
}


fn main( agrc: u32, argv: string ){

    let num1: u32 = 50;

    switch(a){
        0 .. 50 ->
            break;
        _       ->
            println(5);
    }


}